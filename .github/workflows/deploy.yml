name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch: # allow manual trigger from GitHub UI

concurrency:
  group: deploy-production
  cancel-in-progress: false # never cancel a running deploy mid-flight

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: "596596146505"
  ECR_API_REPO: ytgrabber-green-api-lambda
  ECR_WORKER_REPO: ytgrabber-green-worker
  ECR_TRANSLATOR_REPO: ytgrabber-green-translator
  CF_STACK_PREFIX: ytgrabber-green

jobs:
  # ─── Job 1: Build & push API Lambda image ────────────────────────────────────
  build-api:
    name: Build API Lambda Image
    runs-on: ubuntu-latest
    outputs:
      image_uri: ${{ steps.push.outputs.image_uri }}
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2

      - uses: pnpm/action-setup@v4
        with: { version: latest }

      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }

      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @workspace/api-server run build

      - name: Build & push API Lambda image
        id: push
        run: |
          TAG="${GITHUB_SHA::8}"
          URI="${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_API_REPO }}:${TAG}"
          docker build \
            --platform linux/amd64 \
            -f Dockerfile.api-lambda \
            -t "$URI" \
            .
          docker push "$URI"

          DIGEST=$(aws ecr batch-get-image \
            --region "${{ env.AWS_REGION }}" \
            --repository-name "${{ env.ECR_API_REPO }}" \
            --image-ids "imageTag=${TAG}" \
            --accepted-media-types "application/vnd.docker.distribution.manifest.v2+json" "application/vnd.oci.image.manifest.v1+json" \
            --query "images[0].imageId.imageDigest" \
            --output text)
          if [ -z "$DIGEST" ] || [ "$DIGEST" = "None" ]; then
            echo "Lambda-compatible manifest was not published for $URI"
            exit 1
          fi

          echo "image_uri=${URI}" >> "$GITHUB_OUTPUT"
          echo "✅ API image: ${URI} (${DIGEST})"

  # ─── Job 2: Build & push Fargate worker image ────────────────────────────────
  build-worker:
    name: Build Fargate Worker Image
    runs-on: ubuntu-latest
    outputs:
      image_uri: ${{ steps.push.outputs.image_uri }}
      batch_job_def: ${{ steps.register.outputs.batch_job_def }}
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2
      - uses: docker/setup-buildx-action@v3

      - name: Build & push worker image
        id: push
        run: |
          TAG="${GITHUB_SHA::8}"
          URI="${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_WORKER_REPO }}:${TAG}"
          docker buildx build \
            --platform linux/amd64 \
            -f artifacts/queue-worker/Dockerfile \
            -t "$URI" \
            --push .
          echo "image_uri=${URI}" >> "$GITHUB_OUTPUT"
          echo "✅ Worker image: ${URI}"

      - name: Register new Batch job definition
        id: register
        run: |
          WORKER_URI="${{ steps.push.outputs.image_uri }}"

          # Get current active job definition
          CURRENT=$(aws batch describe-job-definitions \
            --job-definition-name ${{ env.CF_STACK_PREFIX }}-worker-job \
            --status ACTIVE \
            --query 'sort_by(jobDefinitions, &revision)[-1]' \
            --output json)

          # Use del() to strip ONLY the read-only fields — preserves everything
          # else including platformCapabilities:["FARGATE"], propagateTags, etc.
          NEW_JSON=$(echo "$CURRENT" | jq \
            --arg img "$WORKER_URI" \
            'del(.jobDefinitionArn, .revision, .status, .jobDefinitionName, .containerOrchestrationType) |
             .containerProperties.image = $img')

          # Register it
          RESULT=$(aws batch register-job-definition \
            --job-definition-name ${{ env.CF_STACK_PREFIX }}-worker-job \
            --cli-input-json "$NEW_JSON")

          REV=$(echo "$RESULT" | jq -r '.revision')
          DEF="${{ env.CF_STACK_PREFIX }}-worker-job:${REV}"
          echo "batch_job_def=${DEF}" >> "$GITHUB_OUTPUT"
          echo "✅ Batch job def: ${DEF}"

  # ─── Job 3: Build & push Translator Fargate image ─────────────────────────────
  build-translator:
    name: Build Translator Image
    runs-on: ubuntu-latest
    outputs:
      image_uri: ${{ steps.push.outputs.image_uri }}
      batch_job_def: ${{ steps.register.outputs.batch_job_def }}
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2
      - uses: docker/setup-buildx-action@v3

      - name: Build & push translator image
        id: push
        run: |
          TAG="${GITHUB_SHA::8}"
          URI="${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_TRANSLATOR_REPO }}:${TAG}"
          docker buildx build \
            --platform linux/amd64 \
            -f artifacts/video-translator-service/Dockerfile \
            -t "$URI" \
            --push artifacts/video-translator-service/
          echo "image_uri=${URI}" >> "$GITHUB_OUTPUT"
          echo "✅ Translator image: ${URI}"

      - name: Register new Batch job definition
        id: register
        run: |
          TRANSLATOR_URI="${{ steps.push.outputs.image_uri }}"

          # Get current active job definition
          CURRENT=$(aws batch describe-job-definitions \
            --job-definition-name ${{ env.CF_STACK_PREFIX }}-translator-job \
            --status ACTIVE \
            --query 'sort_by(jobDefinitions, &revision)[-1]' \
            --output json)

          NEW_JSON=$(echo "$CURRENT" | jq \
            --arg img "$TRANSLATOR_URI" \
            'del(.jobDefinitionArn, .revision, .status, .jobDefinitionName, .containerOrchestrationType) |
             .containerProperties.image = $img |
             (.containerProperties.resourceRequirements[] | select(.type == "MEMORY").value) = "15000" |
             (.containerProperties.resourceRequirements[] | select(.type == "GPU").value) = "1"')

          RESULT=$(aws batch register-job-definition \
            --job-definition-name ${{ env.CF_STACK_PREFIX }}-translator-job \
            --cli-input-json "$NEW_JSON")

          REV=$(echo "$RESULT" | jq -r '.revision')
          DEF="${{ env.CF_STACK_PREFIX }}-translator-job:${REV}"
          echo "batch_job_def=${DEF}" >> "$GITHUB_OUTPUT"
          echo "✅ Batch job def: ${DEF}"

  # ─── Job 4: Deploy full stack ─────────────────────────────────────────────────
  deploy:
    name: Deploy to AWS
    runs-on: ubuntu-latest
    needs: [build-api, build-worker, build-translator]
    # No environment gate — secrets are repo-level, no approval required
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2

      # pnpm + node needed because deploy-serverless.ps1 runs pnpm build internally
      - uses: pnpm/action-setup@v4
        with: { version: latest }

      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }

      - run: pnpm install --frozen-lockfile

      # Write env file and inject fresh runtime settings.
      - name: Write .env.green
        run: |
          printf '%s' "${{ secrets.ENV_GREEN_CONTENT }}" > /tmp/.env.green
          sed -i "s|^YOUTUBE_BATCH_JOB_DEFINITION=.*|YOUTUBE_BATCH_JOB_DEFINITION=${{ needs.build-worker.outputs.batch_job_def }}|" /tmp/.env.green
          sed -i '/^GEMINI_API_KEY_[2-9]=/d;/^GEMINI_API_KEY_10=/d' /tmp/.env.green
          if [ -n "${{ secrets.GEMINI_API_KEY }}" ]; then
            if grep -q '^GEMINI_API_KEY=' /tmp/.env.green; then
              sed -i 's|^GEMINI_API_KEY=.*|GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}|' /tmp/.env.green
            else
              printf '\nGEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}\n' >> /tmp/.env.green
            fi
          fi
          if grep -q '^WEBSITE_AUTH_PASSWORD=' /tmp/.env.green; then
            sed -i 's|^WEBSITE_AUTH_PASSWORD=.*|WEBSITE_AUTH_PASSWORD=kalkiavatar@2026#vm|' /tmp/.env.green
          else
            printf '\nWEBSITE_AUTH_PASSWORD=kalkiavatar@2026#vm\n' >> /tmp/.env.green
          fi
          if grep -q '^BHAGWAT_PASSWORD=' /tmp/.env.green; then
            sed -i 's|^BHAGWAT_PASSWORD=.*|BHAGWAT_PASSWORD=bhagwat@2026clips|' /tmp/.env.green
          else
            printf '\nBHAGWAT_PASSWORD=bhagwat@2026clips\n' >> /tmp/.env.green
          fi
          if grep -q '^YOUTUBE_QUEUE_PRIMARY_JOB_TYPES=' /tmp/.env.green; then
            sed -i 's|^YOUTUBE_QUEUE_PRIMARY_JOB_TYPES=.*|YOUTUBE_QUEUE_PRIMARY_JOB_TYPES=clip-cut,best-clips,bhagwat-analyze,bhagwat-render|' /tmp/.env.green
          else
            printf '\nYOUTUBE_QUEUE_PRIMARY_JOB_TYPES=clip-cut,best-clips,bhagwat-analyze,bhagwat-render\n' >> /tmp/.env.green
          fi
          if grep -q '^SUBTITLES_FORCE_LAMBDA=' /tmp/.env.green; then
            sed -i 's|^SUBTITLES_FORCE_LAMBDA=.*|SUBTITLES_FORCE_LAMBDA=true|' /tmp/.env.green
          else
            printf '\nSUBTITLES_FORCE_LAMBDA=true\n' >> /tmp/.env.green
          fi
          if grep -q '^YTDLP_COOKIES_FILE=' /tmp/.env.green; then
            sed -i 's|^YTDLP_COOKIES_FILE=.*|YTDLP_COOKIES_FILE=/tmp/yt-cookies.txt|' /tmp/.env.green
          else
            printf '\nYTDLP_COOKIES_FILE=/tmp/yt-cookies.txt\n' >> /tmp/.env.green
          fi
          if grep -q '^TRANSLATOR_BATCH_JOB_DEFINITION=' /tmp/.env.green; then
            sed -i "s|^TRANSLATOR_BATCH_JOB_DEFINITION=.*|TRANSLATOR_BATCH_JOB_DEFINITION=${{ needs.build-translator.outputs.batch_job_def }}|" /tmp/.env.green
          else
            printf '\nTRANSLATOR_BATCH_JOB_DEFINITION=${{ needs.build-translator.outputs.batch_job_def }}\n' >> /tmp/.env.green
          fi
          if grep -q '^TRANSLATOR_BATCH_JOB_QUEUE=' /tmp/.env.green; then
            sed -i "s|^TRANSLATOR_BATCH_JOB_QUEUE=.*|TRANSLATOR_BATCH_JOB_QUEUE=${{ env.CF_STACK_PREFIX }}-gpu-queue|" /tmp/.env.green
          else
            printf '\nTRANSLATOR_BATCH_JOB_QUEUE=${{ env.CF_STACK_PREFIX }}-gpu-queue\n' >> /tmp/.env.green
          fi
          echo ".env.green ready ($(wc -l < /tmp/.env.green) lines)"

      # Run the existing deploy script. It handles CloudFormation, S3, and CloudFront.
      - name: Deploy (CloudFormation + S3 + Lambda)
        shell: pwsh
        run: |
          try {
            ./deploy/aws-serverless/deploy-serverless.ps1 `
              -Region "${{ env.AWS_REGION }}" `
              -Prefix "${{ env.CF_STACK_PREFIX }}" `
              -SkipImageBuild `
              -ImageUri "${{ needs.build-api.outputs.image_uri }}" `
              -EnvFilePath "/tmp/.env.green"
          } catch {
            Write-Host "::error::DEPLOY_SCRIPT_ERROR $($_.Exception.Message)"
            if ($_.ScriptStackTrace) {
              Write-Host "::error::DEPLOY_SCRIPT_STACK $($_.ScriptStackTrace)"
            }
            throw
          }

      - name: Summary
        run: |
          echo "## ✅ Deployed to Production" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| | |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${GITHUB_SHA::8}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **API image** | \`${{ needs.build-api.outputs.image_uri }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Worker image** | \`${{ needs.build-worker.outputs.image_uri }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Batch job** | \`${{ needs.build-worker.outputs.batch_job_def }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Translator image** | \`${{ needs.build-translator.outputs.image_uri }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Translator Batch job** | \`${{ needs.build-translator.outputs.batch_job_def }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Live** | https://videomaking.in |" >> $GITHUB_STEP_SUMMARY
