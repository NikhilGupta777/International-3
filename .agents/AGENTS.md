# Workspace Rules

- Before every commit, you must run type checks, build steps, and tests.
- Only if those checks pass, proceed to commit and push.
- After every edit to the codebase, verify the changes don't break the build before continuing.
