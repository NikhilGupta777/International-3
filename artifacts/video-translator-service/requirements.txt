# 
# AWS Batch GPU Worker — requireme──────────────────────────────────────────────────────────────────────────────nts.txt
# NOTE: PyTorch is installed via CUDA wheel in Dockerfile, NOT listed here.
# ──────────────────────────────────────────────────────────────────────────────

# Base worker runtime
boto3>=1.34.0
google-genai>=1.73.0
pydantic>=2.7.0
httpx>=0.27.0
aiofiles>=24.1.0
python-dotenv>=1.0.1

# ASR — faster-whisper on GPU (CTranslate2 backend)
faster-whisper>=1.1.0
ctranslate2>=4.4.0

# ASR premium — WhisperX forced alignment
whisperx>=3.3.0

# Speaker diarization (optional, requires HF_TOKEN)
pyannote.audio>=3.1.0

# Translation runtime (google-genai already listed above)

# Audio / Video processing
soundfile>=0.12.1
librosa>=0.10.2
pydub>=0.25.1
opencv-python-headless>=4.9.0
numpy==1.26.4

# Vocal separation
demucs>=4.0.1

# TTS — Coqui XTTS v2
TTS==0.22.0
phonemizer>=3.3.0

# TTS fallbacks
edge-tts>=6.1.9
gtts>=2.5.0

# Compatibility patches for Transformers + XTTS
transformers==4.40.2
tokenizers>=0.19.0
