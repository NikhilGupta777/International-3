# AWS Batch GPU Worker requirements
# NOTE: PyTorch + torchaudio are installed via CUDA wheel in Dockerfile.
# NOTE: LatentSync deps are installed lazily at first runtime.
# NOTE: demucs and pyannote are optional and installed lazily too (USE_DEMUCS / MULTI_SPEAKER flags).

# ── Core worker runtime ───────────────────────────────────────────────────────
boto3==1.34.162
google-genai==1.73.0
pydantic==2.10.6
httpx==0.28.1
aiofiles==24.1.0
python-dotenv==1.0.1

# ── Transcription (AssemblyAI API — no heavy model download) ─────────────────
assemblyai==0.30.0

# ── Audio / Video processing ──────────────────────────────────────────────────
soundfile==0.12.1
librosa==0.10.2.post1
pydub==0.25.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
numba==0.59.1
llvmlite==0.42.0
ffmpeg-python==0.2.0
scipy

# ── CosyVoice core deps — sourced from upstream FunAudioLLM/CosyVoice ─────────
# Reference: https://github.com/FunAudioLLM/CosyVoice/blob/main/requirements.txt
transformers==4.48.3
huggingface_hub==0.24.6
diffusers==0.29.2
accelerate==0.31.0
conformer==0.3.2
einops==0.8.0
encodec==0.1.1
gdown==5.1.0
grpcio==1.57.0
grpcio-tools==1.57.0
hydra-core==1.3.2
hydra-colorlog==1.2.0
HyperPyYAML==1.2.3
inflect==7.3.1
librosa==0.10.2.post1
lightning==2.2.4
matplotlib==3.7.5
networkx==3.1
omegaconf==2.3.0
onnx==1.16.0
openai-whisper==20231117
pandas
phonemizer==3.3.0
protobuf==4.25
pyarrow==18.1.0
pyworld==0.3.4
rich==13.7.1
rootutils
ruamel.yaml>=0.17.28,<0.18.0
scipy
seaborn
tensorboard==2.14.0
torchmetrics
Unidecode==1.3.8
wetext==0.0.4
wget==3.2
x-transformers==2.11.24

# ── TTS fallbacks ─────────────────────────────────────────────────────────────
edge-tts==6.1.9
gtts==2.5.0

# ── Vocal separation (Demucs) ─────────────────────────────────────────────────
# Baked in so no pip install runs during user jobs.
demucs==4.0.1
