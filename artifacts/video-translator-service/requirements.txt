# AWS Batch GPU Worker — requirements.txt
# NOTE: PyTorch + torchaudio are installed via CUDA wheel in Dockerfile, NOT listed here.
# ──────────────────────────────────────────────────────────────────────────────

# Base worker runtime
boto3>=1.34.0
google-genai>=1.73.0
pydantic>=2.7.0
httpx>=0.27.0
aiofiles>=24.1.0
python-dotenv>=1.0.1

# ASR — AssemblyAI API
assemblyai>=0.30.0

# Speaker diarization (optional, requires HF_TOKEN)
pyannote.audio>=3.1.0

# Audio / Video processing
soundfile>=0.12.1
librosa>=0.10.2
pydub>=0.25.1
opencv-python-headless>=4.9.0
numpy==1.26.4

# Vocal separation
demucs>=4.0.1

# CosyVoice 3.0 dependencies (CosyVoice repo installed as editable from /model-cache/CosyVoice)
# These are required by CosyVoice internally:
transformers>=4.41.0
huggingface_hub>=0.23.0
diffusers>=0.29.0
accelerate>=0.30.0
conformer>=0.3.2
encodec>=0.1.1
hyperpyyaml>=1.2.2
inflect>=7.0.0

# TTS fallbacks (used when voice_clone=false or CosyVoice fails)
edge-tts>=6.1.9
gtts>=2.5.0

# LatentSync 1.6 deps (installed from LatentSync/requirements.txt at runtime)
# huggingface_hub already listed above for checkpoint download
