# AWS Batch GPU Worker requirements
# NOTE: PyTorch + torchaudio are installed via CUDA wheel in Dockerfile.
# NOTE: LatentSync and MuseTalk deps are installed lazily at first runtime.
# NOTE: demucs and pyannote are optional and installed lazily too (USE_DEMUCS / MULTI_SPEAKER flags).

# ── Core worker runtime ───────────────────────────────────────────────────────
boto3==1.34.162
google-genai==1.73.0
pydantic==2.10.6
httpx==0.28.1
aiofiles==24.1.0
python-dotenv==1.0.1

# ── Transcription (AssemblyAI API — no heavy model download) ─────────────────
assemblyai==0.30.0

# ── Audio / Video processing ──────────────────────────────────────────────────
soundfile==0.12.1
librosa==0.10.2.post1
pydub==0.25.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
numba==0.59.1
llvmlite==0.42.0
ffmpeg-python==0.2.0

# ── CosyVoice core deps (repo cloned in Dockerfile) ──────────────────────────
transformers==4.48.3
huggingface_hub==0.24.6
diffusers==0.29.2
accelerate==0.31.0
conformer==0.3.2
encodec==0.1.1
hyperpyyaml==1.2.2
inflect==7.0.0
omegaconf==2.3.0
openai-whisper==20231117

# ── TTS fallbacks ─────────────────────────────────────────────────────────────
edge-tts==6.1.9
gtts==2.5.0
